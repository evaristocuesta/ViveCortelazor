@using Markdig

@inject IViewLocalizer Localizer

@{
    ViewData["Title"] = $"{Localizer["Cookies"].Value} - Vive Cortelazor - Sierra de Aracena";
    ViewData["Robots"] = "noindex,follow";
}
<div class="container">
    <div class="article">
        <markdown content="@Html.Raw(Localizer["Cookies Text"].Value)"></markdown>
    </div>
</div>